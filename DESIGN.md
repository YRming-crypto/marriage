# Design System

## Direction

照片优先的暖色婚恋产品界面，采用稳重的编辑式层级与清楚的产品控件。整体保持明亮、克制和可扫描，不使用玻璃拟态、装饰渐变、胶囊堆叠或嵌套卡片。

设计参数：布局变化 5/10，动效强度 4/10，信息密度 5/10。

## Color

色彩策略采用 Full Palette，但主操作仍只有一个暖红强调色。

- 品牌红：`oklch(0.55 0.18 18)`，用于主要按钮和关键选择。
- 深莓色：`oklch(0.35 0.09 12)`，用于标题和品牌文字。
- 青绿色：`oklch(0.53 0.09 185)`，用于 AI 身份、信息提示和辅助状态。
- 安心绿：`oklch(0.52 0.10 155)`，用于审核通过和聊天开放。
- 页面底色：接近无彩的品牌轻微染色白，不使用奶油或沙色。
- 正文与弱文字必须分别达到 WCAG AA，不使用浅灰正文。

## Typography

- 使用系统中文无衬线字体，优先 `Microsoft YaHei UI`、`Microsoft YaHei`、`PingFang SC`。
- H1 最大 64px，手机最大 42px；卡片标题控制在 20px 左右。
- 正文 16-18px，行高 1.65；辅助文字最小 14px。
- 标题使用 700-800 字重，不使用负字距，不使用大段全大写。
- H1-H3 使用 `text-wrap: balance`，长文使用 `text-wrap: pretty`。

## Layout

- 主内容最大宽度 1240px，使用 4px 基础间距系统。
- 桌面导航保持一行，主头部不超过 80px；手机使用清楚的菜单按钮。
- 首页首屏为全宽真实人物照片，文字直接叠在图像上，不放入卡片。
- 首页会员区采用有节奏的照片网格；匹配大厅保持统一卡片以利比较。
- 人物详情桌面使用固定照片栏与内容栏，手机改为单列和全宽操作。
- 筛选、聊天和建档是真实工具，可以使用有边界的工作区；普通页面区块不做浮动卡片。

## Components

- 按钮：主要、次要、文字、图标四类，高度至少 44px，圆角 6px。
- 人物卡：4:5 照片、基本资料、最多三个标签和两个清晰操作；演示标识弱化。
- 标签：用于真实属性，不作为装饰；避免一行出现过多胶囊。
- 状态提示：完整边框或柔和背景，搭配图标和文字，不使用粗侧边色条。
- 表单：标签始终可见，输入框高度至少 48px，焦点环清晰。

## Motion

- 首屏签名动效：照片轻微聚焦，标题和操作按层级进入，总时长不超过 800ms。
- 列表动效：只对同一组人物卡或入口做一次轻微错峰，总时长不超过 500ms。
- 状态动效：收藏、页签、聊天就绪和申请发送使用 150-300ms 反馈。
- 不做全站逐区块上浮，不做弹跳、视差长滚动和循环装饰动画。
- 所有动画都有 `prefers-reduced-motion` 降级，内容默认可见，不能依赖动画脚本才能显示。

## Responsive Rules

- 1100px：会员网格从四列降为三列。
- 900px：导航切换为菜单，筛选改抽屉，详情和聊天改为单列。
- 680px：会员大厅为单列横向人物卡，表单与操作按钮全宽。
- 420px：精简次要标签和图标，但保留姓名、年龄、城市与主要操作。
